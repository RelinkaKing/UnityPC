using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainConfig{

    //是否拉取数据
	public static bool isFetchDb = true;
    //是否下载模型
    public static bool isDownloadModel = true;
    //是否是测试服
    public static bool isTestServer = true;
    public static bool isShowToggle = false;
    public static bool isAdaptionUI = false;
    public static bool isOnline=false;
    //是否录制书签
    public static bool isSaveFixBookmarkMode = false;

    public static bool isLoadLocalSignNew = false;
    public static bool isFullScreen = true;
}
